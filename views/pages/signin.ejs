<!DOCTYPE html>
<html>
    <link rel="stylesheet" href="/css/login.css">
    <body class="bgImage">
        
    </body>
    <form id="loginForm" class="loginForm" onsubmit="return checker" id="loginForm">

        <div class="container">
            <h1 class="judul">
                Agrishop
            </h1>
            <h2 class="judul">
                Daftar
            </h2>
            <p class="judul">
                Sudah punya akun? <a href="login">Masuk</a>
            </p>
        </div>
        <div class="container">
            <label for="email"><b>Email</b></label>
            <input type="text" id="email" placeholder="Masukkan Email" name="email" required >
            
            <label for="username"><b>Nama Lengkap</b></label>
            <input type="text" id="username" placeholder="Masukkan Nama Lengkap" name="username" required>

            <label for="password"><b>Kata Sandi</b></label>
            <div class="pass">
            <input type="password" id="password" placeholder="Masukkan Kata Sandi"name="password"  required>
            <input type="checkbox" onclick="showPass()">
            </div>

            <label for="password"><b>Ulangi Kata Sandi</b></label>
            <div class="pass">
            <input type="password" id="passwordRepeat" placeholder="Ulangi Kata Sandi"name="passwordRepeat"  required>
            <input type="checkbox" onclick="showPassRepeat()">
            </div>

            <script>
                function showPass(){
                    var x = document.getElementById("password");
                    if (x.type === "password"){
                        x.type = "text";
                    }else{
                        x.type = "password"
                    }
                }
                function showPassRepeat(){
                    var x = document.getElementById("passwordRepeat");
                    if (x.type === "password"){
                        x.type = "text";
                    }else{
                        x.type = "password"
                    }
                }
            </script>
            <div class="pass">
            <label for="remember">Ingat Saya</label>
            <input type="checkbox" checked="checked" name="remember" id="remember">
            </div>

            <button type="submit" class="button" onclick="checker()">Daftar</button>

            <script type="/js/signin.js"></script>

            </div>
            <div class="container">
                <p class="result"></p>
            </div>
        
    </form>
</html>